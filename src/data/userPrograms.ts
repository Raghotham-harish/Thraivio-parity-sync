import type { UserProgram } from "@/types/userProgram";

/**
 * Temporary compatibility export.
 *
 * My Programs will use real enrolled-program data
 * once the Enrollment/Purchase module is integrated.
 *
 * Until then, no fake/random enrollment data
 * should be shown to the user.
 */
export const userPrograms: UserProgram[] = [];